OOP의 절차(프로세서)
	1. 
어떤 객체를 (class, instance)를 만들지 결정한다.
	2. 
클래스 객체가 속성과 기능을 결정한다.


            여기서의 속성은(멤버변수),동작은(멤버메소드)

Main
[속성] 없음 - 메인메소드와 클래스사이에 아무것도 없기때문에
[기능] main()

Member
 [속성] 명사 : id, name, ssn, pass;
 [기능] 동사 : 회원정보 보여주기 info( );

Account
[속성] String accountNum; today; int money;
[기능] generatorNum() // 계좌번호 생성
            today() // 오늘날짜생성
            info() // 계좌정보 생성
            
객체들사이에서 메세지를 주고받도록 만들어준다.
- 클래스와 인스턴스사이에서

가독성